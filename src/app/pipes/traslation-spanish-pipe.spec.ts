import { TraslationSpanishPipe } from './traslation-spanish-pipe';

describe('TraslationSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new TraslationSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
